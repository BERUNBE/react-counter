export const increment = (type) => {
    return{
        type:'INCREMENT'
    }
}

export const decrement = (type) => {
    return{
        type:'DECREMENT'
    }
}

export const double = (type) => {
    return{
        type:'DOUBLE'
    }
}

export const halve = (type) => {
    return{
        type:'HALVE'
    }
}

export const reset = (type) => {
    return{
        type:'RESET'
    }
}